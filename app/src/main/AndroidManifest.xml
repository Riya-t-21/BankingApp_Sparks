<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bankingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_riya"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_riya_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BankingApp">
        <activity
            android:name="splash_screen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
     />
        <activity
            android:name="sendTouser"
            android:exported="false" />
        <activity
            android:name="user_data"
            android:parentActivityName=".user_list"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="false" />
        <activity
            android:name="history_list"
            android:parentActivityName=".user_list"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="false" />
        <activity
            android:name="user_list"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="false" />
        <activity
            android:name="MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>